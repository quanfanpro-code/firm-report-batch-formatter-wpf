# 批处理互鉴实施计划

执行方式：读取 executing-plans.md，按用户自主授权串行实施并推送当前 main。

目标：纠正封面误判并保存检查结果。架构：现有分类器与流水线局部扩展。技术：.NET 10、System.Text.Json、现有 xUnit。方案见 ../specs/2026-09-12-批处理互鉴-design.md。

- [x] 需求与方案：已确定自动兼容、显式纠正、实际结果落盘及原稿保护边界。
- [x] RED：新建 开发测试/文档快照导出测试/批处理互鉴测试.cs；断言短正文显式覆盖后 `IsPureCoverDocument == false`，流水线成功、失败、取消后对应检查记录存在且内容一致。
- [x] GREEN：修改 Contracts/RequestContract.cs、ResponseContract.cs、Core/FirmDocumentClassifier.cs、DocumentPipeline.cs、MainWindow.xaml/.cs；新建 Core/检查记录服务.cs。复用已有 GateCheck 与 LogEventContract。
- [x] VERIFY／审查：新增定向 RED 后一次完整 `dotnet test 开发测试/文档快照导出测试/文档快照导出测试.csproj`；要求失败 0，缺失历史样本的既有跳过如实记录。核对 XAML 绑定、取消记录、不可覆盖记录与记录失败提示。
- [x] 交付：更新 README 与审查记录，提交推送并核对远端，不打包、不扩测。
