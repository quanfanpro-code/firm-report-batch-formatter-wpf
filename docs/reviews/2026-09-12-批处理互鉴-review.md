# 批处理互鉴审查与交付

六阶段：需求澄清、方案设计、实施计划、TDD 实施、验收审查已完成；用户授权自主决策并提交推送，按授权交付。

处理方式增加自动、纯封面、正文；默认沿用旧规则。显式选择仅决定是否为纯封面，正文是否含封面仍由原流程决定。界面在批次开始时快照该选择。

每次执行保存已有响应、门禁及日志事件；批次收尾保存实际尝试文件和取消状态。新记录使用 CreateNew，不覆盖旧记录；记录写入异常通过 AuditWarning 返回并在界面显示，文档成功状态保留。检查记录明确 Word 实测未执行。原有文档处理和数据格式不改。

新增定向测试在修改前 5 项按预期失败；实现后本地旧基线完整测试 95 通过。推送前发现云端已有 6 个后续提交，已将本次改动接到 f59e86a 之后，保留其全部修复；因合入内容改变了处理与校验代码，对最终合并版本执行一次验证，150 通过、0 失败、0 跳过，含 WPF 与验证工具编译。新增用例覆盖手动覆盖自动判断、默认兼容、成功失败取消记录、旧记录不覆盖及原稿不变。

审查覆盖单文件与批次调用、日志逐次隔离、失败门禁保留、UI 线程、保存异常及文件竞争。批次汇总采用内存列表，沿用现有顺序批处理；未增加重复验证或新依赖。

最终合并版本的测试作为阶段 4—6 的共同证据。此次未进行 Word 版式实测和真实 GUI 点击验收，不把编译通过写成这两项通过。原入口保留，用户文件及旧产物不纳入提交；新增检查记录文件已加入 Git 忽略规则。
